using System.Linq;
namespace myleetcode
{
    public class Solution414
    {
        public int ThirdMax(int[] nums)
        {
            // Array.Sort<int>(nums);
            // System.Collections.Generic.IEnumerable<int> uniquenums = nums.Distinct<int>();
            // int[] nums1 = uniquenums.ToArray();
            // if (nums1.Length >= 3) return nums1[nums1.Length - 3];
            // else return nums1[nums1.Length - 1];
            return 0;
        }
    }
}
